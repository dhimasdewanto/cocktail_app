import 'package:flutter/material.dart';

/// Fragment to view random drink.
class RandomFragment extends StatelessWidget {
  const RandomFragment({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Random'),
    );
  }
}